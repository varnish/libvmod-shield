varnishtest "Test shield vmod"

server s1 {
       rxreq
       txresp
} -start

varnish v1 -vcl+backend {
	import shield from "${vmod_topbuild}/src/.libs/libvmod_shield.so";
} -start

client c1 {
	txreq -url "/"
	rxresp
}

client c1 -run
